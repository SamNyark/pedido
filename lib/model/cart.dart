class CartModel {
  String? title;
  String? id;
  String? image;
  int? quantity;
  int? price;
  String? time;
  bool? isExit;

  CartModel({this.title, this.id, this.image, this.quantity, this.price, this.isExit, this.time});
}
